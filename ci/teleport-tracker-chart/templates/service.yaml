apiVersion: v1
kind: Service
metadata:
  name: {{ include "teleport-tracker-chart.fullname" . }}
  labels:
    {{- include "teleport-tracker-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "teleport-tracker-chart.selectorLabels" . | nindent 4 }}
