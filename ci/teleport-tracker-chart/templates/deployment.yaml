apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "teleport-tracker-chart.fullname" . }}
  labels:
    {{- include "teleport-tracker-chart.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "teleport-tracker-chart.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "teleport-tracker-chart.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}" #{{ .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
